export interface FullXidmet {
  count: number,
  edv: string,
  expenseId: number,
  totalAmount: number,
  nvNo: string,
  amount: number,
  isSelected: boolean,
  fullRefCode: string,
  emptyRefCode: string,
  expenseText: string
}
